<port> = port du serveur

Si argument 2 présent : <adresse> = arg2
Sinon                 : <adresse> = 127.0.0.1

Se connecter sur <adresse> sur le <port>

Tant que <vrai>
	Créer un thread : écouter le serveur et afficher les messages qu'il envoit
	Envoyer tout ce que l'utilisateur tape au clavier au serveur
Fin Tant que
